import { Controller } from '@nestjs/common';

// Placeholder controller so FunnelsModule can register a controllers array
// and the app boots. Task 8 replaces this with real routes.
@Controller('funnels')
export class FunnelsController {}
